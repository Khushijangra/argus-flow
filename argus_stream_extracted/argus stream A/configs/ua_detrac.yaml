# UA-DETRAC Configuration

data:
  dataset: ua_detrac
  data_dir: ../../data
  train_split: train
  val_split: val

stream_a:
  hidden_dim: 512

training:
  batch_size: 2048
  learning_rate: 0.0001
  epochs: 50
